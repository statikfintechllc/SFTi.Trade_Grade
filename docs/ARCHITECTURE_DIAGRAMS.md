# SFTi P.R.E.P - Authentication Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         GitHub Pages                             │
│              https://statikfintechllc.github.io/                 │
│                    SFTi.Trade_Grade/                             │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │
        ┌───────────────────────┴───────────────────────┐
        │                                               │
        ▼                                               ▼
┌──────────────┐                               ┌──────────────┐
│  index.html  │                               │   Callback   │
│  (Main App)  │                               │   Handler    │
└──────┬───────┘                               └──────────────┘
       │                                       /system/auth/
       │                                         callback/
       ├── Loads ──────────┐
       │                   │
       ▼                   ▼
┌─────────────────┐  ┌─────────────────┐
│ cors-bypass.js  │  │static-backend.js│
│                 │  │                 │
│ • Direct Fetch  │  │ • OAuth Device  │
│ • Service Worker│  │ • OAuth Web     │
│ • Iframe Proxy  │  │ • Token Mgmt    │
│ • JSONP         │  │ • Rate Limit    │
└────────┬────────┘  └────────┬────────┘
         │                    │
         └──────────┬─────────┘
                    │
                    ▼
            ┌───────────────┐
            │  cors-sw.js   │
            │ Service Worker│
            │ (Interceptor) │
            └───────┬───────┘
                    │
                    ▼
        ┌───────────────────────┐
        │    External APIs       │
        │                        │
        │  • GitHub API          │
        │  • GitHub Copilot      │
        │  • Azure Inference     │
        └────────────────────────┘
```

## Authentication Flows

### Device Flow (Recommended) ⭐

```
┌──────────┐                    ┌──────────────┐                ┌──────────┐
│   User   │                    │ Static Backend│                │  GitHub  │
└────┬─────┘                    └───────┬───────┘                └────┬─────┘
     │                                  │                              │
     │ 1. Click "Connect"               │                              │
     ├────────────────────────────────►│                              │
     │                                  │                              │
     │                                  │ 2. Request Device Code       │
     │                                  ├────────────────────────────►│
     │                                  │                              │
     │                                  │ 3. Return device_code +      │
     │                                  │    user_code + verify_uri    │
     │                                  │◄────────────────────────────┤
     │                                  │                              │
     │ 4. Display Code & URL            │                              │
     │◄─────────────────────────────────┤                              │
     │                                  │                              │
     │ 5. User opens verify_uri         │                              │
     │  in browser and enters code      │                              │
     ├──────────────────────────────────┼────────────────────────────►│
     │                                  │                              │
     │                                  │ 6. Poll for token (every 5s) │
     │                                  ├────────────────────────────►│
     │                                  │                              │
     │                                  │ 7. Return access_token       │
     │                                  │    (after user authorizes)   │
     │                                  │◄────────────────────────────┤
     │                                  │                              │
     │ 8. Show success                  │                              │
     │◄─────────────────────────────────┤                              │
     │                                  │                              │
     │ 9. Ready to use AI!              │                              │
     │                                  │                              │
```

**Why Device Flow?**
- ✅ No callback URL needed
- ✅ No CORS issues
- ✅ Works on ANY static site
- ✅ More secure (no client secret)
- ✅ Better UX (no redirects)

### Web Flow (Alternative)

```
┌──────────┐      ┌──────────────┐      ┌──────────┐      ┌──────────┐
│   User   │      │  Main App    │      │ Callback │      │  GitHub  │
└────┬─────┘      └───────┬──────┘      └────┬─────┘      └────┬─────┘
     │                    │                   │                  │
     │ 1. Click           │                   │                  │
     │   "Connect"        │                   │                  │
     ├──────────────────►│                   │                  │
     │                    │                   │                  │
     │                    │ 2. Redirect to    │                  │
     │                    │    GitHub OAuth   │                  │
     │                    ├───────────────────┼─────────────────►│
     │                    │                   │                  │
     │ 3. Authorize       │                   │                  │
     ├────────────────────┼───────────────────┼─────────────────►│
     │                    │                   │                  │
     │                    │                   │ 4. Redirect with │
     │                    │                   │    code          │
     │◄───────────────────┼───────────────────┤◄─────────────────┤
     │                    │                   │                  │
     │                    │                   │ 5. Exchange code │
     │                    │                   │    for token     │
     │                    │                   ├─────────────────►│
     │                    │                   │                  │
     │                    │                   │ 6. Return token  │
     │                    │                   │◄─────────────────┤
     │                    │                   │                  │
     │                    │ 7. Redirect back  │                  │
     │                    │    with token     │                  │
     │◄───────────────────┤◄──────────────────┤                  │
     │                    │                   │                  │
```

**Limitations:**
- ⚠️ Requires callback URL
- ⚠️ CORS limitations on GitHub Pages
- ⚠️ Needs client secret
- ⚠️ More complex flow

## CORS Bypass Strategies

```
┌─────────────────────────────────────────────────────────────┐
│                    CustomCorsWidget.fetch()                  │
└─────────────────────────────────────────────────────────────┘
                             │
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐   ┌────────────────┐   ┌───────────────┐
│ Strategy 1:   │   │ Strategy 2:    │   │ Strategy 3:   │
│ Direct Fetch  │   │ Service Worker │   │ Iframe Proxy  │
│               │   │                │   │               │
│ Try CORS      │   │ SW intercepts  │   │ Sandboxed     │
│ normally      │   │ and proxies    │   │ iframe with   │
│               │   │                │   │ postMessage   │
└───────┬───────┘   └────────┬───────┘   └───────┬───────┘
        │                    │                    │
        │ Success?           │ Success?           │ Success?
        ├─Yes───────►┐       ├─Yes───────►┐       ├─Yes───────►┐
        └─No───►     │       └─No───►     │       └─No───►     │
                     │                     │                     │
                     ▼                     ▼                     ▼
            ┌────────────────┐    ┌────────────────┐    ┌──────────────┐
            │   Strategy 4:  │    │  Strategy 5:   │    │   Return     │
            │   JSONP        │    │  Recommend     │    │   Response   │
            │   (GET only)   │    │  Device Flow   │    │              │
            └────────┬───────┘    └────────────────┘    └──────────────┘
                     │
                     │ Success?
                     ├─Yes───────►┘
                     └─No───► Error
```

## Token Storage & Management

```
┌──────────────────────────────────────────────────────┐
│                    LocalStorage                       │
├──────────────────────────────────────────────────────┤
│                                                       │
│  • oauth_client_id          → GitHub OAuth Client ID │
│  • oauth_client_secret      → Client Secret (opt)    │
│  • oauth_state             → CSRF protection         │
│  • copilotToken            → GitHub Copilot token    │
│  • copilotTokenExpiry      → Expiry timestamp        │
│  • githubToken             → GitHub PAT token        │
│  • device_code_data        → Device flow data        │
│                                                       │
└──────────────────────────────────────────────────────┘
                         │
                         │ Synced via
                         ▼
┌──────────────────────────────────────────────────────┐
│                BroadcastChannel                       │
│              'sfti-backend'                           │
├──────────────────────────────────────────────────────┤
│                                                       │
│  Syncs tokens and state across browser tabs          │
│                                                       │
└──────────────────────────────────────────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────────────────────────┐
│                    Request Flow                          │
└─────────────────────────────────────────────────────────┘
                         │
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐  ┌─────────────┐  ┌─────────────┐
│   CSRF       │  │    Rate      │  │   Token     │
│   Check      │  │   Limiting   │  │   Expiry    │
│              │  │              │  │   Check     │
│ Validate     │  │ Max 60 req   │  │             │
│ state param  │  │ per minute   │  │ Auto-expire │
└──────┬───────┘  └──────┬───────┘  └──────┬──────┘
       │                 │                 │
       └────────┬────────┴────────┬────────┘
                │                 │
                ▼                 ▼
        ┌───────────────┐  ┌─────────────┐
        │   Caching     │  │    CSP      │
        │               │  │   Headers   │
        │ 5 min TTL     │  │             │
        │ Auto-cleanup  │  │ Security    │
        └───────────────┘  └─────────────┘
```

## File Structure

```
SFTi.Trade_Grade/
│
├── index.html                    # Main app (loads all modules)
│
├── _config.yml                   # Jekyll configuration
│
├── system/
│   ├── js.on/
│   │   ├── cors-bypass.js       # ⭐ Custom CORS widget
│   │   ├── cors-sw.js           # ⭐ Service worker
│   │   ├── static-backend.js    # ⭐ Custom backend
│   │   ├── auth.js              # ✅ Fixed redirect_uri
│   │   ├── config.js            # Configuration
│   │   ├── utils.js             # Utilities
│   │   ├── toast.js             # Toast notifications
│   │   ├── models.js            # Model management
│   │   ├── chat.js              # Chat interface
│   │   ├── ai.js                # AI engine
│   │   └── init.js              # App initialization
│   │
│   └── auth/
│       └── callback/
│           └── index.html       # ✅ OAuth callback handler
│
└── docs/
    ├── CUSTOM_CORS_AND_OAUTH.md # 📚 Full documentation
    └── ... (other docs)

Legend:
⭐ New files (v3.0)
✅ Updated files
📚 Documentation
```

## Quick Reference

### Initialize Custom Backend
```javascript
// Automatically initialized on page load
// Access via: window.CustomStaticBackend
```

### Start Device Flow
```javascript
const result = await CustomStaticBackend.startDeviceFlow();
// Display result.userCode to user
// User goes to result.verificationUri
const token = await CustomStaticBackend.pollDeviceToken(
    result.deviceCode, 
    result.interval
);
```

### Make API Request
```javascript
const response = await CustomStaticBackend.apiRequest(
    'https://api.githubcopilot.com/chat/completions',
    {
        method: 'POST',
        body: JSON.stringify({ ... })
    }
);
```

### CORS Bypass Fetch
```javascript
const response = await CustomCorsWidget.fetch(url, options);
```

---

**Version:** 3.0.0  
**Last Updated:** 2026-01-21  
**Author:** SFTi LLC
